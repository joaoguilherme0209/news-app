# URL base da API (backend) — sem barra no final
# Desenvolvimento (backend rodando localmente):
VITE_API_URL=http://localhost:3000
# Produção (após deploy no Render/Railway): use a URL fornecida, ex: https://seu-app.onrender.com